# =============================================================================
# Hashnode GraphQL Client Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Publication Configuration
# -----------------------------------------------------------------------------

# Your Hashnode publication host (subdomain or custom domain)
# This is REQUIRED for all API operations
#
# Examples:
#   - Hashnode subdomain: yourblog.hashnode.dev
#   - Custom domain: blog.yourdomain.com
#
# Find your publication host in Hashnode dashboard → Blog Dashboard → General
HASHNODE_PUBLICATION_HOST=yourblog.hashnode.dev


# -----------------------------------------------------------------------------
# OPTIONAL: Authentication (for Draft Management)
# -----------------------------------------------------------------------------

# Hashnode Personal Access Token
# Required ONLY for draft-related operations (getDrafts function)
#
# To generate a token:
#   1. Go to Hashnode Settings → Developer
#   2. Click "Generate New Token"
#   3. Copy the token and paste below
#
# Permissions needed: Read access to drafts
# HASHNODE_ACCESS_TOKEN=your-personal-access-token-here


# -----------------------------------------------------------------------------
# OPTIONAL: Webhook Configuration
# -----------------------------------------------------------------------------

# Webhook secret for signature verification
# Required ONLY if using webhook integration
#
# To set up webhooks:
#   1. Go to Hashnode dashboard → Settings → Webhooks
#   2. Click "Add Webhook"
#   3. Enter your webhook URL (must be HTTPS)
#   4. Generate and copy the secret key
#   5. Select events to subscribe to
#   6. Paste the secret below
#
# Security: ALWAYS verify webhook signatures using verifyWebhookSignature()
# HASHNODE_WEBHOOK_SECRET=your-webhook-secret-here


# =============================================================================
# Additional Configuration (Advanced)
# =============================================================================

# Custom API endpoint (ONLY change if using self-hosted Hashnode instance)
# Default: https://gql.hashnode.com
# HASHNODE_API_URL=https://gql.hashnode.com

# Request timeout in milliseconds
# Default: 10000 (10 seconds)
# HASHNODE_TIMEOUT_MS=10000


# =============================================================================
# Environment-Specific Examples
# =============================================================================

# Development Environment
# HASHNODE_PUBLICATION_HOST=dev-blog.hashnode.dev
# HASHNODE_ACCESS_TOKEN=dev_token_here
# HASHNODE_WEBHOOK_SECRET=dev_secret_here

# Staging Environment
# HASHNODE_PUBLICATION_HOST=staging-blog.hashnode.dev
# HASHNODE_ACCESS_TOKEN=staging_token_here
# HASHNODE_WEBHOOK_SECRET=staging_secret_here

# Production Environment
# HASHNODE_PUBLICATION_HOST=blog.yourcompany.com
# HASHNODE_ACCESS_TOKEN=prod_token_here
# HASHNODE_WEBHOOK_SECRET=prod_secret_here


# =============================================================================
# Usage Notes
# =============================================================================

# 1. Copy this file to .env in your project root
# 2. Uncomment and fill in the required values
# 3. Never commit .env to version control (it's in .gitignore)
# 4. For production, use environment variables in your hosting platform:
#    - Vercel: Project Settings → Environment Variables
#    - Netlify: Site Settings → Build & Deploy → Environment
#    - AWS: Use Systems Manager Parameter Store or Secrets Manager
#    - Docker: Use docker-compose.yml env_file or --env-file flag

# =============================================================================
# Security Best Practices
# =============================================================================

# ✅ DO:
#   - Keep .env file in .gitignore
#   - Use different tokens/secrets for dev/staging/production
#   - Rotate tokens regularly
#   - Use platform-specific secret management in production
#   - Verify webhook signatures on every request

# ❌ DON'T:
#   - Commit .env or tokens to Git
#   - Share tokens in public channels
#   - Use production tokens in development
#   - Hardcode secrets in your code
#   - Skip webhook signature verification
